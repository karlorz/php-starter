# https://docs.docker.com/compose/compose-file/05-services/#image
services:
  php:
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        ARG COMPOSER_VERSION: 2.6.5
        ARG PHP_VERSION: 8.2
        ARG BOX_VERSION: 4.5.1
        ARG BOX_CHECKSUM: c24c400c424a68041d7af146c71943bf1acc0c5abafa45297c503b832b9c6b16
        ARG GIT_EMAIL: "seb@local.fr"
        ARG GIT_USERNAME: "seb"
        ARG ALPINE_VERSION: 3.19
#    expose:
#      - 8000
    ports:
      - target: 8000
        published: 8000
#    configs:
#      - source: maconfig
#        target: /dockerconfig
#        mode: 0444
    volumes:
      - type: bind
        source: ./
        target: /app
    user: "0:1000"
    restart: unless-stopped
    depends_on:
      - postgres
  postgres:
    # service name is the hostname
    # @todo : utiliser un fichier de config pour la version de postgres
    image: postgres:16.1
    environment:
      # @todo : changer ces valeurs pour la prod, utiliser un fichier de config
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: my_symfony
    # utile pour la communication à l'extérieur du container uniquement @todo vérifier si utile
    expose:
      - 5432
    ports:
      - target: 5432
        published: 5432
    #    volumes:
    #      - type: volume
    #        source: postgres_data
    #        target: /var/lib/postgresql/data
    restart: unless-stopped

# https://docs.docker.com/compose/compose-file/08-configs/

#configs:
#  maconfig:
#    file: ./dockerconfig

# https://docs.docker.com/compose/compose-file/10-fragments/

volumes:
  postgres_data:
